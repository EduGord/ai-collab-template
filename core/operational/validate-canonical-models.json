{
  "routineId": "validate-canonical-models",
  "description": "Ensure all core schemas and canonical data models are present and documented",
  "trigger": "on-schema-update",
  "steps": [
    "Scan /schemas/ for missing required models",
    "Ensure each schema has metadata, usage links, and status",
    "Ensure docs/design-principles.md explains canonical schema layout"
  ],
  "status": "required",
  "$schema": "schemas/default-schema.json"
}