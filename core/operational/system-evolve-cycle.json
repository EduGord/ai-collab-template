{
  "trigger": "after-phase-completion",
  "steps": [
    "review agent reflections",
    "scan project memory for structural gaps",
    "append proposed features to next phase template"
  ],
  "description": "Agent self-evolution cycle after major plan completions",
  "$schema": "schemas/default-schema.json"
}