{
  "trigger": "post-agent-execution",
  "steps": [
    "check if task had deviation or failure",
    "log reflection",
    "update agent-state or checklist if needed"
  ],
  "description": "Post-task reflection mechanism to improve agent behavior",
  "$schema": "schemas/default-schema.json"
}