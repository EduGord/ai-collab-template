{
  "trigger": "on-agent-session-begin",
  "steps": [
    "Inspect agent type and purpose",
    "Load active files from memory index",
    "Filter by relevance to plan objective",
    "Apply route rules and return context scope"
  ],
  "description": "Routes session context for LLMs based on task and plan"
}
