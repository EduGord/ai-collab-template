{
  "trigger": "agent-task-audit",
  "steps": [
    "load all known routines",
    "cross-check against known intents and pipelines",
    "identify uncovered routines",
    "suggest generation of new routines"
  ],
  "description": "This routine enables agents to identify missing behaviors in the system.",
  "$schema": "schemas/routine-schema.json"
}