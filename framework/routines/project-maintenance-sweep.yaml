routine:
  name: "project-maintenance-sweep"
  frequency: "weekly"
  actions:
    - review open tasks
    - check agent health
    - validate pipelines
    - check test coverage (coveragebot)
    - trigger automated review (reviewkeeper)
    - log all progress
    - verify memory_bank.json + agent_log.json structure and freshness
