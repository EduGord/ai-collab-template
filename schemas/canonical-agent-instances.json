{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Canonical Agent Instances",
  "type": "object",
  "properties": {
    "agentId": {
      "type": "string",
      "description": "Unique identifier of the agent instance"
    },
    "role": {
      "type": "string",
      "description": "Functional role of the agent in the system"
    },
    "capabilities": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of capabilities the agent has"
    }
  },
  "required": [
    "agentId",
    "role",
    "capabilities"
  ]
}